import http from 'node:http';import fs from 'node:fs/promises';import path from 'node:path';
const root=path.resolve('.preview');const types={'.html':'text/html; charset=utf-8','.js':'text/javascript','.css':'text/css','.wasm':'application/wasm','.hex':'text/plain','.png':'image/png'};
http.createServer(async(req,res)=>{try{const url=new URL(req.url,'http://127.0.0.1');const p=path.resolve(root,'.'+decodeURIComponent(url.pathname==='/'?'/index.html':url.pathname));if(!p.startsWith(root+path.sep))throw Error();res.setHeader('Content-Security-Policy',"default-src 'none'; script-src 'self' 'wasm-unsafe-eval'; worker-src 'self'; style-src 'self'; img-src 'self'; connect-src 'self' https://a1-planck.quantus.cat https://rpc1-mainnet.quantus.com https://rpc2-mainnet.quantus.com https://sqm.quantus.com https://sub2.quantus.com; object-src 'none'; base-uri 'none'; form-action 'none'; frame-ancestors 'none'");res.setHeader('Cache-Control','no-store');res.setHeader('Content-Type',types[path.extname(p)]??'application/octet-stream');res.end(await fs.readFile(p));}catch{res.writeHead(404);res.end('Not found');}}).listen(Number(process.env.QUANTUS_PREVIEW_PORT??4179),'127.0.0.1',()=>console.log('Preview ready on configured localhost port'));
